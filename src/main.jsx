import React from "react";
import ReactDOM from "react-dom/client";
import PortfolioSite from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PortfolioSite />
  </React.StrictMode>
);
